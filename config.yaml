# config.yaml

coinbase:
  quote_asset: "USD"

risk:
  bankroll_usd: 100
  max_trade_pct: 5
  notes: "target_notional_usd should track bankroll * max_trade_pct"

brackets:
  tp_offset_bps: 8 # <-- softened to 8 bps for testing
  min_stop_pct: 0.10 # 10 bps floor for SL
  post_only: true
  target_notional_usd: 5

binance:
  enabled: false
  notes: "Deprecated"

kraken:
  enabled: false
  notes: "Deprecated"
